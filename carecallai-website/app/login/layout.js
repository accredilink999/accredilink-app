export const metadata = {
  title: "Log In to CareCallAI",
  description: "Log in to your CareCallAI account. Access your care management dashboard, rota, care logs and compliance tools.",
  robots: { index: false, follow: false },
};

export default function LoginLayout({ children }) {
  return children;
}
